/* Aural Lab — Korean UI strings (future expansion).
   This dict ships EMPTY on purpose: the language selector only offers
   한국어 once keys are added here (see js/i18n.js AVAILABLE). Until then
   every lookup falls back to English. Mirror the keys in en.js when
   translating; follow the shared glossary
   (D:\1Claude\music-terminology-glossary.md) for music terms.
   Musical symbols, note names and the F / J keys are not translated. */
const I18N_KO={};
if(typeof module!=="undefined") module.exports=I18N_KO;
